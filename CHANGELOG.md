# 更新紀錄

## 2026-07-15 v10

### [Bug fixed]AOI 座標落在 Panel 外

- AOI 座標依影像原始範圍換算後若落在偵測到的 Panel polygon 外，會改依產品四角做透視座標映射，修正 G0F、W0F 與 B0F tile 框到 Panel 外而得到全零遮罩的問題。
- 修正後座標若仍不在 Panel 內，或 tile 沒有任何有效 Panel 區域，會明確中止該 Panel 推論，不再把全零熱力圖誤當成 AI OK。

### [功能調整]OMIT 像素格紋平滑

- 設定頁新增 1366x768 圖片專用的像素格紋平滑開關與 Gaussian kernel；預設關閉，啟用後只影響 OMIT 灰塵／氣泡遮罩，不改變其他解析度或模型分數。

### [維運]更新包發布腳本

- `promote_update.sh` 可自動選取 staging 中最新版 code-only ZIP，啟動更新服務後會檢查程序與 HTTP 回應；失敗時輸出 log，避免顯示成功但 Client 無法下載。

## 2026-07-15 v9

### [Bug fixed]重新推論套用 AOI OK 快速返回

- 網頁「重新推論」現在會將原紀錄的 `machine_judgment` 傳入推論器，修正 AOI 原判定為 OK 時未進入 `AOI_OK_SKIP`、仍執行 OMIT、5-lighting 預處理與 ScratchClassifier 的問題。
- 有／無 GPU lock 的 rerun 分支皆套用相同行為；AOI NG 或具有 AOI／forced 座標時仍維持完整推論。

## 2026-07-15 v8

### [Bug fixed]AOI OK 快速返回

- 當 `grid_tiling_enabled=false`、AOI 座標推論開啟、Client 機檢判定為 OK 且報告沒有 NG／forced 座標時，保留 MARK／QJPG 結果後直接返回，不再載入 OMIT、執行 5-lighting 預處理、模型路由或 ScratchClassifier。
- 推論 log 會標記 `AOI_OK_SKIP`，各 AI 階段時間顯示為 0，方便確認快速路徑生效。

## 2026-07-15 v7

### [Bug fixed]AOI-only Grid 與耗時計錄

- `grid_tiling_enabled=false` 時不再先建立全面板 Grid tiles 再移除；AOI 報告為 OK、沒有 NG／forced 座標時會維持 0 個推論 tile。
- Panel 總耗時 log 改列前置、預處理、Tile 準備、GPU 推論與後處理／收尾，避免分項時間與總耗時無法對照。

## 2026-07-15 v6

### [Bug fixed]MARK R5 辨識

- 改善 `R5` 在低對比、雜點、網紋及點陣傾斜畫面下被誤判的問題；QJPG 回覆欄位維持不變。

## 2026-07-14 v8

### [Bug fixed]CAPI13 相機方向修正

- hostname 完整等於 `capi13` 時，檢測前會將輸入圖片旋轉 180 度，以修正反裝相機造成的方向問題。
- 其他 hostname 與原始圖片檔不受影響；更新包只需覆蓋程式並重啟服務。

## 2026-07-14 v7

### [Bug fixed]炸彈線段座標匹配

- 炸彈線段改用產品座標中的有限線段距離判定，支援橫線、豎線與斜線，避免只用矩形範圍造成誤匹配。
- AOI tile 的炸彈後處理沿用相同的線段容忍帶規則，並補上三種方向的回歸測試。

### [維運]資料庫與規格內熱力圖清理

- 清理過期 tile 前先刪除 `scratch_rescue_review` 子資料，避免外鍵錯誤讓整個每日清理交易回滾。
- `within_spec_inference` run 目錄納入既有 90 天 heatmap 保留策略，並在 cleanup log 顯示刪除數量。

## 2026-07-14 v6

### [Bug fixed]AOI-only 產品邊界參考圖

- AOI-only 推論即使報告未列出 W0F，也會優先用同資料夾的 W0F 計算產品邊界；W0F 只作為抓邊參考，不會增加模型推論或輸出 lighting 數量。
- Polygon 擬合會避開四角交界取樣並拒絕無法穩定形成直線的邊緣；成像不穩時會改試下一個 lighting，避免歪斜邊界被當成有效結果。

## 2026-07-14 v5

### [功能調整]模型庫顯示完整訓練設定

- 模型庫的資訊頁補上 precision、特徵鄰域聚合、特徵域清洗範圍／保留比例、tile step、前處理套用時機與完整流程、訓練資料來源，以及各 PANEL 的 INNER／EDGE 切片設定。
- 舊模型若沒有保存新欄位，畫面會明確標示「舊模型未記錄」；只有能由既有流程確定的 3×3、關閉清洗、512px tile step 等項目才會標示相容預設，避免把推定值誤當成實際紀錄。

## 2026-07-14 v4

### [功能調整]訓練 PANEL 的 INNER／EDGE 配比

- Step 2 的 PANEL 清單改為固定高度可捲動，來源路徑以單行省略顯示，避免單筆長路徑把選片欄位撐高。
- 每片已選 PANEL 可分別勾選是否切 INNER、EDGE，預設兩者皆勾選；可用不同 PANEL 補足各區域樣本，降低 INNER／EDGE tile 數量失衡。
- 完整訓練仍要求至少有一片提供 INNER、另一片提供 EDGE；局部重訓只要求選定 PT 所需的區域。實際選項會隨訓練 job 儲存並在前處理階段套用。

## 2026-07-14 v3

### [功能調整]特徵清洗範圍

- 完整訓練的 Step 2 新增「特徵清洗範圍」，可選擇僅清洗 inner（預設）或同時清洗 inner + edge；既有 bundle 與未指定範圍的訓練維持 inner-only。
- edge 清洗會使用相同的保留比例，畫面提示先以 99.5%～99.9% 保守測試；實際範圍會寫入 manifest、Step 6 報告與重訓紀錄，局部／單模型重訓仍沿用原 bundle。

## 2026-07-14 v2

### [功能調整]特徵清洗保留比例

- 完整訓練的 Step 2 可自行設定特徵清洗保留比例，範圍為 90%～100%、預設 99%；例如保留 97% 約代表移除最離群的 3%，設定 100% 則不移除特徵。
- 實際比例會套用到 inner 模型的密度清洗並寫入 manifest 與 Step 6 報告；局部／單模型重訓仍固定沿用原 bundle 設定。

## 2026-07-14 v1

### [實驗功能]PatchCore 特徵聚合與特徵域清洗

- 完整訓練的進階設定新增 `3×3 / 5×5` 特徵鄰域聚合，以及「保守特徵清洗」開關；預設維持原本的 `3×3 + 關閉`，不影響既有訓練結果。
- 保守清洗只在 inner 模型建立 coreset 前執行，以 cosine kNN（k=30）保留密度最高的 99% patch 特徵；OMIT／PINIGBI 灰塵遮罩仍只用於推論後續判定，不會加入訓練資料清洗。
- 訓練結果會記錄各模型的原始、保留、移除特徵數與實際參數；局部／單模型重訓強制沿用原 bundle 設定，避免同一模型包混用不同聚合方式。實驗模型仍需用固定測試集重放並確認 threshold 後再手動啟用。

## 2026-07-13 v2

### [Bug fixed]跨 Tile 邊界氣泡遮罩

- 修正低對比氣泡落在 AOI Tile 左右邊界時，因影像裁切不完整而未被圈入灰塵遮罩的問題；系統會依異常點位置補做邊界偵測，並只補回跨越原 Tile 邊界的氣泡。
- 即使原 Tile 已找到其他氣泡，也會繼續檢查邊界氣泡；內部 Tile 與未跨越邊界的候選維持既有判定。

## 2026-07-13 v1

### [Bug fixed]規格內灰塵候選判定

- AOI NG tile 的有效點候選若全部被灰塵遮罩排除，現在會以 0 點納入 Panel 規格比較並可判定 OK；明細頁會說明灰塵排除數與轉為 OK-I 的原因。原本就沒有點候選、或含 MARK/不檢測區排除的情況維持既有判定。

### [功能調整]統計報表預設分頁

- 統計報表首頁改為「AI 推論紀錄」，並將其移至次分頁第一順位；RIC Report 保留原有功能，調整至第二順位。

### [Feature]Debug 點圖黑白結果分流

- Debug 點圖偵測不再只顯示自動選中的單一點類；同一次測試會分別呈現黑點與白點的候選數、最大尺寸、Overlay、Mask、Diff Heatmap 與候選明細，方便確認背景紋理是在哪個點類分支被抓入。
- 保留既有 Debug API 頂層結果欄位，新增黑點／白點分組資料，避免影響既有呼叫；正式推論的規格內判定邏輯維持不變。

## 2026-07-10 v10

### [Feature]訓練機手動資料夾來源

- 訓練 Step 2 新增「手動資料夾」模式，可輸入機種 ID 與 batch 根目錄，掃描第一層 panel 資料夾，不再依賴本機最近推論紀錄。
- 掃描結果會顯示各 panel 可辨識的 lighting 與完整度；建立 job 前會再次驗證路徑，並要求人員確認資料為正常訓練樣本。
- 手動來源類型與 batch 根目錄會寫入訓練 job 及模型 `manifest.json`，方便後續追查模型訓練資料來源。

## 2026-07-10 v9

### [Bug fixed]部署包啟動依賴

- 修正 release ZIP 漏放 `capi_edge_cv.py`、熱力圖、前處理、資料集匯出與 scratch 後濾等啟動依賴模組，避免新 PC 解壓後啟動時出現 `ModuleNotFoundError`。

## 2026-07-10 v8

### [Bug fixed]灰塵 two-stage 過檢

- 修正 WGF50500 灰塵 two-stage 判定中，候選框膨脹後把高 Heatmap 核心旁邊的局部紋理誤當 REAL 缺陷的問題；現在能翻回 REAL_NG 的 feature 必須落在未膨脹的高 Heatmap 核心附近，避免 AOI 旁邊底紋造成過檢。
- 灰塵設定頁補充 `Heatmap 熱區取前 X%` 也會作為 two-stage REAL feature 的核心範圍，方便現場調參時理解影響。

## 2026-07-10 v7

### [Bug fixed]模型庫啟用與 HM 儀表板名稱

- 修正模型庫啟用時，資料庫相對路徑與 `server_config.yaml` 絕對路徑無法比對，造成舊 bundle 殘留且重啟後仍載入舊模型；啟用、停用與刪除現在統一以解析後路徑比對。
- Server 啟動時若設定中殘留多個新架構 bundle，會優先載入資料庫標記為 active 的 bundle，不再直接取清單第一筆。
- HM 儀表板 threshold 畫面會依 active bundle 的訓練來源將 `STANDARD` 顯示為 `U0F00000`；模型檔與 YAML 內部鍵仍維持 `STANDARD`。

## 2026-07-10 v6

### [Bug fixed]無 NG 樣本訓練與 HM 訓練名稱

- 修正訓練資料沒有 NG tile 時，空的 `test/anormal` 造成 anomalib 十個 unit 全部失敗；現在會以 synthetic anomaly 建立驗證集，PatchCore 模型仍只使用審核通過的 OK tile 訓練，NG 指標會標示無資料。
- HM 新線體的 Step 5 unit 卡片、失敗摘要、訓練 log 與 Step 6 結果表會顯示 `U0F00000-inner/edge`，內部模型檔與路由鍵仍維持 `STANDARD-inner/edge`。

## 2026-07-10 v5

### [Bug fixed]HM 光源顯示名稱

- 推論紀錄、V3 推論紀錄、訓練 tile 審核與單圖除錯畫面會依實際來源檔名將 `STANDARD` 顯示為 `U0F00000`；模型路由、門檻與資料庫鍵仍維持 `STANDARD`，確保既有模型相容。

## 2026-07-10 v4

### [Bug fixed]HM 訓練畫面光源名稱

- HM 新線體訓練 tile 審核頁會依來源檔名將 `STANDARD` 顯示為 `U0F00000`，避免操作人員誤認光源；內部訓練與模型對應仍維持 `STANDARD`，不影響既有模型相容性。

## 2026-07-10 v3

### [Bug fixed]灰塵偵測(跨 Tile 氣泡)

- 修正同一顆大型氣泡因 AOI Tile 裁切位置不同，部分 Tile 遮罩完整、部分 Tile 仍判定 NG 的問題；當氣泡碰到 Tile 左右邊界時，系統會以左右平移視窗補抓完整氣泡，再將遮罩投影回原 Tile。

## 2026-07-10 v2

### [Bug fixed]灰塵偵測(大型氣泡)

- 新增貼近拔貼表面的大型低對比斜橢圓氣泡辨識，完整遮罩氣泡內部與尾端；同時以橢圓形狀、面積、凸度及表面位置限制，避免把大型矩形亮區誤判為氣泡。

## 2026-07-10 v1

### [Bug fixed]灰塵偵測(氣泡)

- 修正氣泡貼近拔貼亮邊時只遮罩到上半部的問題；系統會依已辨識的表面邊界補齊氣泡下半部，避免熱區被誤判為真實缺陷。

## 2026-07-09 v2

### [Bug fixed]HM 新線體檔名相容

- 支援 HM 新線體無底線時間戳檔名，`G0F00000083754`、`R0F00000083753`、`W0F00000083751`、`WGF50500083752`、`B0F00000083756` 會對應既有畫面前綴。
- `U0F00000083755` 會對應既有 `STANDARD` 畫面；`PINIGBI0083748` 維持灰塵圖用途，不納入一般 lighting 推論。
- 重投片判斷新增「同一畫面前綴重複」檢查，HM 新線體即使圖片總數未超過上限，也會只選同前綴最新圖片推論。
- AOI Report 解析支援新線體多行格式：獨立一行 `NG` 後的座標列會納入 AOI 座標推論；機檢 `OK` 且無座標時會視為無 AOI 座標，避免格式差異造成誤判。

## 2026-07-09 v1

### [Bug fixed]CAPI 通訊

- CAPIHM無炸彈機制 傳輸為空時，Server 會忽略圖片路徑前的空白保留欄位，避免把 `;;` 誤判成路徑內容造成 `DIR_NOT_FOUND`。

## 2026-07-08 v4

### [功能優化]不檢測區熱力圖降權

- MARK 不檢測區核心由直接歸零改為依 `no_detect_soft_decay_min_weight` 降權，保留極低熱力權重並維持外圈 soft decay 行為。

## 2026-07-08 v3

### [Bug fixed]推論服務

- 修正服務停止或重啟期間仍有 AOI 請求完成時，背景儲存 executor 已關閉導致 `cannot schedule new futures after shutdown` 被記成推論錯誤的問題；此情境會改為同步保存結果，不再污染 Client 回覆。

### [功能優化]規格內判定

- 規格內明細頁在沒有 Panel 點數可加總時，會顯示 OpenCV 初步候選數、灰塵遮罩排除數、MARK/不檢測區排除數與最後有效點數，避免只看到「沒有可加總」而不清楚候選點被遮罩排除。

## 2026-07-08 v2

### [Bug fixed]產線更新同步

- 修正自動更新完成後重啟服務可能回落到系統 `/usr/bin/python3` 的問題；updater 會把目前使用的 Python 傳給 installer 與啟動腳本，啟動腳本也會優先偵測現場 CAPI conda 環境，避免 OpenCV 依賴缺失。

## 2026-07-08 v1

### [功能優化]推論紀錄

- 推論紀錄詳情頁在回覆時間後顯示收到 Client 的原始請求字串與回覆 Client 的完整結果字串，方便排查通訊內容。

### [功能優化]規格內判定

- 規格內黑點/白點 OpenCV 判定會套用 MARK 與手動不檢測區遮蔽，避免 MARK 文字或拔貼區殘留被算成有效點數或非點狀殘留。
- 規格內明細圖新增不檢測區遮罩/疊圖，方便確認候選點被排除的位置。

## 2026-07-07 v10

### [功能優化]不檢測區熱力圖降權

- MARK 與手動不檢測區核心仍維持硬遮罩，外圈新增 heatmap soft decay 降權，降低 MARK/拔貼區周圍熱力擴散造成過檢的機率。
- 設定頁新增 MARK 外擴、MARK soft decay、手動不檢測區 soft decay 與最低權重參數，方便現場依實際擴散範圍微調。

## 2026-07-07 v9

### [功能優化]AOI 機檢座標

- AOI 機檢座標設定新增「炸彈區域強制偵測」開關；開啟後，當 Client 提供炸彈座標但 AOI Report 未提供相近座標時，系統會補切該座標 tile 進行既有推論與 BOMB 比對。
- 若 AOI Report 已提供 Client 炸彈座標附近的點，系統會沿用原本 AOI 座標推論流程，不額外補切 tile；推論 log 會記錄 AOI 已涵蓋與補切數量。

## 2026-07-07 v8

### [Bug fixed]刮痕分類器

- 修正設定頁切換刮痕分類器模型後，Debug 批次測試與推論可能沿用記憶體中舊模型的問題；模型路徑或 DINOv2 路徑變更後會重新載入分類器。
- 修正 `scratch_safety_multiplier` 若舊資料型別為整數時，輸入 `0.95` 可能被讀成 `0`，導致生效門檻變成 `0.0000` 的問題。

## 2026-07-07 v7

### [Bug fixed]推論紀錄

- 修正 AOI Report 已解析到缺陷座標時，本地推論紀錄仍可能把機檢判定存成 `OK` 的問題；重新推論覆蓋既有紀錄時也會套用相同修正，回覆客戶端的 AI 判定維持既有邏輯。

## 2026-07-07 v6

### [Bug fixed]灰塵偵測(氣泡)

- 氣泡遮罩會排除產品水平邊界下方的低對比候選，避免低通補洞流程把底部表面背景誤當成氣泡。

## 2026-07-07 v5

### [Bug fixed]灰塵偵測(氣泡)

- 氣泡遮罩新增低通去條紋偵測流程，改善直條紋背景下淡氣泡無法形成有效候選的問題。
- 通過條件的破碎氣泡輪廓會以凸包填滿，避免只標到氣泡外圈而漏掉中心區域。

## 2026-07-07 v4

### [Bug fixed]灰塵偵測(氣泡)

- 低對比氣泡遮罩會填滿通過條件的外輪廓，避免只標到氣泡邊緣或局部小點，提升 dust mask 對整顆氣泡的覆蓋。

## 2026-07-07 v3

### [Bug fixed]灰塵偵測(氣泡)

- 修正既有 DB 內 `dust_detect_bubbles_enabled` 若曾以文字方式儲存為 `"True"` / `"False"`，設定頁仍顯示文字輸入框的問題；系統會依設定結構自動辨識為開關。
- 使用開關更新舊文字型布林參數時，會同步修正 DB 參數型別，後續不再回到手動輸入模式。

## 2026-07-07 v2

### [Bug fixed]灰塵偵測(氣泡)

- `dust_detect_bubbles_enabled` 在設定頁改為開關操作，避免現場人員用文字手動輸入 `True` / `False`。
- 低對比氣泡遮罩改為同時偵測亮斑與暗斑；開啟後推論明細會顯示 `Bub:n`，方便確認氣泡遮罩分支是否實際執行。

## 2026-07-07 v1

### [功能優化]灰塵偵測(氣泡)

- 灰塵偵測新增可選的低對比氣泡/大片暗斑遮罩，開啟後會把符合幾何條件的表面氣泡併入 OMIT dust mask，再沿用既有 COV / two-stage 判定。
- 設定頁的灰塵偵測頁籤新增 `dust_detect_bubbles_enabled` 開關，預設關閉以避免改變現場既有判定。

## 2026-07-06 v4

### [功能調整]產線更新同步

- 新增 `promote_update.sh`，第一台設備可一鍵完成本機安裝、健康檢查、發布 `latest.json`、啟動更新檔案服務與驗證公開狀態。
- 新增 `setup_auto_update_client.sh`，第二、三台設備可一鍵建立定期檢查 cron，並支援立即執行一次更新。
- 更新安裝/回滾腳本的權限處理，確保自動更新相關腳本解壓後可直接執行。

## 2026-07-06 v3

### [Bug fixed]產線更新同步

- `install_patch.sh` 的預設健康檢查 URL 改為 production 使用的 `http://127.0.0.1/api/version`，避免 port 80 設備更新成功後被誤判為健康檢查失敗。

## 2026-07-06 v2

### [實驗功能]產線更新同步

- 新增實驗版自動更新 agent，可從內網 `latest.json` 檢查新版 patch ZIP，下載後驗證 checksum 並呼叫既有安裝流程。
- 新增更新主機發布流程，可由 patch ZIP 產生 `latest.json`，供其他實驗設備定期拉取。
- `install_patch.sh` 支援 updater 呼叫時的自動回滾開關，健康檢查失敗時可自動執行 rollback。
- 新增實驗 SOP，說明更新主機、實驗機 dry run、實際套用、排程檢查與停用方式。

## 2026-07-06 v1

### [Bug fixed]RIC Report

- AI 推論紀錄的每日推論趨勢圖改用百分比作為折線資料，避免標籤顯示比例但折線仍依筆數高低繪製，造成比例上升時線條反而下降。
- AI 推論紀錄的每日推論趨勢圖新增 `AI 漏檢率` 折線，沿用統計分析的漏檢 Review 分類口徑，只計入 `AI漏放` 與 `Dust 誤濾`。
- RIC Report 與 AI 推論紀錄的日期篩選改用工廠生產日曆，每日統計區間為當日 07:30 到隔日 07:30。
- 即時儀表板與推論紀錄首頁的當班統計改為白班 07:30~19:30、夜班 19:30~07:30。

## 2026-07-03 v2

### [功能調整]RIC Report

- 混淆矩陣 x 軸顯示改為現場用語：`實際Defect`、`C值`、`A值`、`額外復活`。
- AI 推論紀錄新增 `AI 判 OK` 比率卡片，每日趨勢圖會顯示 AI OK 線與各線每日百分比，判定分布圖會直接顯示各區塊百分比。
- 頁尾版本號改為每次頁面 render 時讀取目前版本資訊，避免更新 `VERSION` 後仍顯示舊版號。
- 版本資訊以 `VERSION` 檔為主，避免舊的 `release_manifest.json` 壓過新版號。

### [Bug fixed]通訊協議

- QJPG 回覆在最終判定為 `OK` / `OK-i` 時，只回傳炸彈座標匹配的缺陷資料，不再把已被規格內判定吸收的一般 AI NG 點一起送回 TESTING。

### [新功能]Web 介面

- 瀏覽器標題、頁首與頁尾會直接顯示目前主機 hostname，方便多台電腦部署時辨識正在查看哪一台。

## 2026-07-02 v3

### [功能調整] 機檢結果NG轉OK

- 背景:0702更新後、Testing不再參與機檢判定，當整片 Panel 的有效異常皆為炸彈座標匹配時，推論紀錄的機檢判定會改存為 `OK`，避免人為模擬炸彈樣本被算成 AOI NG。

### [新功能]首頁看板

- 首頁網址可加上 `auto_refresh` 秒數參數自動刷新，例如 `/?auto_refresh=300` 會每 5 分鐘更新一次，允許範圍為 60 到 3600 秒。

## 2026-07-02 v1

### [新功能]刮痕分類器模型網頁配置

- 設定頁修改 `scratch_bundle_path` 時，會自動讀取服務器上已有的 `scratch_classifier*.pkl` 並以下拉選單選取，避免手動輸入路徑。

### [Bug fixed]RIC Report

- 過檢 Review 頁面恢復分類原因分布與已回填原因分布圓餅圖，保留重訓 Pool 欄位與分類篩選功能。

### [Bug fixed]規格內判定

- 規格內計算明細只要有 runtime dust mask，就會輸出灰塵遮罩與灰塵疊圖；即使遮罩為空也會顯示，方便判斷是無灰塵還是遮罩未產生。
- 規格內的非點狀殘留也會套用 dust mask 過濾，避免灰塵區域被新判定方式算成非點狀 NG。

## 2026-07-01 v6

### [Bug fixed]刮痕分類器重訓

- 刮痕分類器重訓改由獨立 subprocess 執行，避免 DINOv2 / LoRA 訓練卡住 Web 狀態查詢。
- 重新進入重訓頁面時，若已有 job 執行中會自動接回目前進度，不再把 `job_already_running` 顯示成訓練失敗。
- 合併 over-review manifest 時會在訓練日誌顯示批次進度，並先寫入暫存檔，成功後再替換正式 `manifest_merged.csv`。
- 訓練 subprocess 會輸出獨立 log 與 summary，頁面可持續顯示進度、完成資訊與錯誤原因。
- LoRA 訓練增加 DataLoader、optimizer、epoch 與 first batch 診斷 log，方便判斷大量資料重訓時卡在哪個階段。

## 2026-07-01 v5

### [Bug fixed]規格內判定

- Debug 推論頁的自動黑點/白點模式，現在會同步檢查黑點與白點兩個分支的非點狀殘留。
- 使用者在 Debug 推論頁調整規格內參數時，非點狀阻擋結果會與 real-time 規格內判定一致，避免線上推論顯示 NG 但測試頁漏顯紅色殘留框。

## 2026-07-01 v4

### [新功能]過檢重訓

- 過檢 Review 保留原本分類/備註功能，並新增「加入重訓 Pool」欄位，可將 AI 過檢、RIC 判定 OK 的 tile 收集為人工核准的重訓候選資料。
- 新增模型庫「重訓 Pool」頁面，可依日期、機種、Lighting、Zone 瀏覽與刪除已收集的重訓資料。
- 模型庫細節頁新增「重訓 Pool」訓練資料入口，可選取 Pool tile 匯入既有 OK tile 訓練池；匯入後只加入訓練清單，不會立即啟動重訓。
- 已加入過訓練清單的 Pool tile 會顯示為已加入，並禁止再次匯入或刪除 Pool 紀錄，避免下一輪訓練重複加入同一張圖片。
- 已加入訓練清單但尚需調整的 Pool tile 可先「移出清單」，清除待訓練 copy 與已匯入標記後再刪除或重新加入，方便處理手動中斷訓練。
- 重訓 Pool 會保留來源 Panel、推論紀錄、DATASTR、畫面 prefix、score 與匯入 bundle/job 資訊，方便追溯資料來源。

### [Bug fixed]畫異預檢

- 重複投片時，畫異預檢會依圖片修改時間選取同一畫面 prefix 的最新圖片，避免拿到舊圖造成誤判。

## 2026-07-01 v3

### [fixed]通訊協議

- 回覆 Client 時會先送出舊版 AOI，再用 CRLF 換行送出新版 QJPG 回覆，讓新舊端程式都能接收判定結果。

### [Bug fixed]規格內判定

- 修正推論後規格內檢查在 dust mask 為空時，產生明細疊圖失敗的問題。
- 空的灰塵遮罩現在會被略過，不會影響原本 AI NG 判定，也不會讓規格內明細顯示 `check_failed`。

## 2026-07-01 v2

### [Bug fixed]炸彈座標匹配

- 新模型訓練產生的 `machine_config.yaml` 將炸彈座標匹配距離預設為 20。
- 推論 log 會顯示炸彈匹配來源、容忍距離、最近炸彈座標與 dx/dy 距離，方便追查是否被 tolerance 吸入。

### [新功能]參數設定權限

- 參數設定頁新增登入保護
- 修改參數時會在修改紀錄中保存操作帳號，可追查誰調整了設定。
- admin 可新增、刪除、編輯一般帳號；一般帳號可進入參數設定與修改參數，但看不到帳號管理頁。
- 暫時隱藏尚未使用的「推論設定」頁籤，降低操作混淆。

## 2026-07-01

### [新功能]模型管理

- 參數設定頁新增「自動切換模型」，可設定機種前 8 碼對應的模型 bundle、預設模型與備註。
- 收到 Client 請求時，系統會依機種系列自動切換唯一啟用中的模型 bundle；找不到對應關係時會使用預設模型。
- 新增自動切換歷史紀錄，方便追查何時切換、切到哪個模型，以及失敗原因。

## 2026-06-29

### [新功能]版本與更新管理

- 系統開始加入版本號，之後可以確認每台設備目前跑的是哪一版。
- 前端新增「更新內容」頁面，使用者可以直接在系統內查看更新紀錄。
- 更新包會附帶更新內容、檔案清單與檢查碼，方便確認檔案是否完整。
- 新增 patch-only 更新包與安裝/回滾腳本，之後小改版可以只更新有變動的檔案。
- 修正部分設備上 shell 腳本因 Windows 換行造成無法執行的問題。

### 影像異常判定

- 影像異常判定改用產品範圍內的資訊計算，降低非產品區域干擾。

## 2026-06-26

### [新功能]QJPG 與影像異常判定

- 新增 QJPG 相關報表與影像異常上限設定，方便追蹤影像異常狀況。

### [Bug fixed]規格內判定

- 灰塵遮罩中的點會被排除，降低灰塵被誤當成規格內點的機率。
- 當系統偵測到仍有明顯漏點時，會避免直接轉成 OK-i，降低誤放風險。

## 2026-06-25

### [新功能]新機種訓練

- 新機種 PatchCore 訓練支援只訓練 layer3 模式，作為特殊情境的選項；原本預設訓練方式不變。
- 訓練時可設定 tile stride，支援重疊取樣，讓訓練資料切圖方式更彈性。
- 訓練流程會把使用的設定寫入模型資訊，之後查模型來源時更清楚。

## 2026-06-23 至 2026-06-24

### [新功能]點圖與白點/黑點偵測

- 點圖偵測演算法更新，改善白點、黑點、淡點與背景差異不明顯時的判斷。
- Debug 頁面改成自動判斷黑點或白點，減少使用者手動選錯的機會。
- 參數設定與 Debug 頁面會依目前偵測模式，只顯示相關參數，畫面更容易理解。

### RIC 報表

- 報表中增加「規格內轉 OK」相關統計，讓使用者更容易看懂 AI 救回的數量來源。

## 2026-06-18

### 規格內判定

- 參數設定頁新增「規格內判定」設定，可以依機種與 defect code 設定判斷規則。
- 系統會記錄規格內判定過程，方便後續追查為什麼某筆資料被轉成 OK-i。
- 規格內判定增加圖片與面板資訊，查問題時不用只看文字紀錄。

### RIC 報表與人工 Review

- 新增「規格內明細」查詢頁，可追蹤哪些資料被判定為規格內。
- RIC Review 新增「規格內誤判」分類，讓人工判讀結果更容易歸類。
- Review 類別調整後，仍保留原本良率與漏檢率計算邏輯，避免報表數字被不小心改變。

## 2026-06-15 至 2026-06-17

### RIC 報表與人工 Review

- RIC 報表新增更多 Review 分類，可把「AI 漏檢」、「規格內」等狀況分得更清楚。
- 報表卡片增加 AOI OK 數量與相關統計，讓報表數字更容易對照。

### 影像前處理與邊緣判定

- 貼近邊緣的 tile 會更正確地走 edge 模型，改善邊緣區域判定。

## 2026-06-03 至 2026-06-05

### 影像前處理與邊緣判定

- 小尺寸產品的邊界偵測、Mark 辨識與切圖流程有強化。
- Mark 或 QR 類區域的排除更完整，減少不該檢測的位置造成誤判。
- 訓練與推論流程支援更多前處理方式，讓不同影像狀況有更合適的處理選項。

### 新機種訓練

- 新機種訓練的 panel 選擇與預覽流程更彈性，訓練前較容易確認資料是否正確。
