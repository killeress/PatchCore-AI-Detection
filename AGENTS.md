# 專案工作偏好

## 打包與發布

- 使用者要求 push 時，預設同步推送至 `inx-git` 與 GitHub `origin`，並確認兩個遠端分支均已更新。
- 使用者要求打包、commit 或 push 時，預設不要執行完整測試套件，避免發布等待過久。
- 單純打包僅做必要的快速檢查：Git diff 格式檢查、打包成功、ZIP 完整性、必要檔案、版本與來源 commit，以及封裝校驗碼。
- 打包時不產生額外的 `.verification.json` 驗證報告檔；必要檢查照常執行，結果直接在回覆中說明。
- 若同時修改程式，只執行與本次變更直接相關的測試，不擴大為完整回歸測試。
- 只有使用者明確要求完整測試時，才執行完整測試套件。
